# PostgreSQL connector for Trino
# Connects to the same PostgreSQL database for unified queries
connector.name=postgresql
connection-url=jdbc:postgresql://postgres:5432/lbpay
connection-user=lbpay
connection-password=lbpay_dev_password
